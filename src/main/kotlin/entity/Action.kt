package entity

/**
 * Enum to distinguish between the four possible [PlayerAction] in the StairCardGame:
 * draw, destroy, combine, or discard
 */
enum class Action {
 DRAW,
 DESTROY,
 COMBINE,
 DISCARD,
 START,
 END
 ;
}
